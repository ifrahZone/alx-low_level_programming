#include "main.h"
#include <string.h>

/**
 * _strlen - function that returns the length of a string
 *
 * @s: string
 *
 * Return: string
 */

int _strlen(char *s)
{
	char c;

	c = *s;
	return (strlen(c));
}
